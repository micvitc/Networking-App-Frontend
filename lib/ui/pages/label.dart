import 'package:flutter/material.dart';

class LabelPage extends StatelessWidget {
  const LabelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFF0C141E),
      body: Center(
        child: Text("LabelPage Page"),
      ),
    );
  }
}
